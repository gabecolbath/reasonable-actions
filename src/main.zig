const std = @import("std");
const cat = @import("categories.zig");

const Allocator = std.mem.Allocator;
const ArrayList = std.ArrayList;

const base_categories_list = @embedFile("categories/base.txt");
const num_categories = 12; 

pub fn main() !void {
    
}
